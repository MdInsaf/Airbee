version: "3.8"

services:
  backend:
    build:
      context: ./aws/backend
      dockerfile: Dockerfile.local
    ports:
      - "8000:8000"
    environment:
      LOCAL_DEV: "true"
      DB_HOST: aws-1-ap-south-1.pooler.supabase.com
      DB_PORT: "5432"
      DB_NAME: postgres
      DB_USER: postgres.dlvrgnslknfehtywtzyh
      DB_PASSWORD: Airbee@@@!!
      DJANGO_SECRET_KEY: local-dev-secret-airbee-2025
      BEDROCK_REGION: us-east-1
      OPENAI_API_KEY: ${OPENAI_API_KEY:-}
    volumes:
      - ./aws/backend:/app
